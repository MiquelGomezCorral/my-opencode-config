interface:
  display_name: "Grill Me"
  short_description: "Stress-test plans through direct questions"
  default_prompt: "Use $grill-me to interrogate this plan until the major decisions, dependencies, assumptions, risks, and validation paths are resolved or explicitly accepted as open risk."
