---
description: Switch ponytail intensity level (lite/full/ultra/off)
---

Switch to ponytail $ARGUMENTS mode. If no level specified, use full. Before any code: does it need to exist at all (YAGNI)? Does the standard library, native platform, or an installed dependency already solve it? Can it be one line? Build the minimum that satisfies the explicit scope. No unrequested abstractions, avoidable dependencies, or boilerplate. Use a `ponytail:` comment only for a deliberate shortcut with a non-obvious ceiling and concrete upgrade trigger.
