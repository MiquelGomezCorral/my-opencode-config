Load the commit-manager skill.

User options: $ARGUMENTS

Review current git changes (`git status` / `git diff`), excluding any files or patterns passed with `--ignore <path-or-glob>`. Never stage or commit ignored paths. Group the remaining changes by logical purpose following the skill's grouping rules, and commit them sequentially.
