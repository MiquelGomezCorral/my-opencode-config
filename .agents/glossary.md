# Glossary

## Terms

- Managed path: a destination replaced by a repository-owned symlink.
- Agent home: global agent-compatible skills installed under `~/.agents`.
- Runtime state: OpenCode data under `~/.local/share`, `~/.local/state`, and `~/.cache`; never versioned.
