# Known Errors

## Errors

- `Unsupported Linux distribution`: only Ubuntu and Debian are currently supported.
- `Dependency has local changes`: remove or preserve changes in the generated OpenCode Quota dependency before rerunning.
- `Docker Compose is unavailable`: install Docker Compose or rerun with `--skip-services`.
- MCP command missing: rerun without `--skip-deps`.
